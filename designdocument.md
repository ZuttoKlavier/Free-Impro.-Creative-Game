# 音乐教学软件产品设计文档

- 项目名称：Free-Impro.-Creative-Game（暂用仓库名称）
- 文档版本：v0.4
- 更新日期：2026-09-23
- 文档性质：已确认产品需求与开发进度记录；学生基础流程及教师课堂准备模块已实现，尚未完成整体验收
- GitHub 仓库：https://github.com/ZuttoKlavier/Free-Impro.-Creative-Game

## 1. 产品目标与文档约定

让学生把生活中的声音与拍摄对象转化为可演奏的动漫角色，由教师组织成可视化的循环音乐作品，支持声音探索、节奏学习与集体创作。

核心流程：录音 → 自动切分 → 试听与编辑 → 拍照生成角色 → 本地保存 → 提交课堂 → 教师编排节奏 → 角色合奏。

本文主体记录已经确认的产品规则。尚未确定的视觉细节、边界行为及技术方案单独列于第 12 节，不作为已经完成或已经验证的能力。本文替代旧版本中与本次确认不一致的规则。

第 13 节单独记录当前实现及未完成部分。需求条目和验收标准描述目标行为，不表示当前版本已经全部实现。

## 2. 用户、设备与规模（已确认）

| 项目 | 要求 |
| --- | --- |
| 学生端 | 主要使用安卓平板 |
| 教师端 | 同时支持 Windows 和 macOS 电脑 |
| 常规课堂规模 | 15 人 |
| 最大课堂规模 | 50 人 |
| 集体演奏 | 支持全班最多 50 位学生的声音同时循环播放 |
| 常规网络 | 使用共同 Wi-Fi |
| 离线场景一 | 无互联网，但仍有共同 Wi-Fi |
| 离线场景二 | 无 Wi-Fi，使用蓝牙传输作品 |

教师电脑统一播放循环音乐。学生平板负责采集、编辑、保存和提交作品；蓝牙用于作品传输，不承担多设备同步音频播放。

当前先以网页实现，使用浏览器客户端与本地课堂服务。安卓真机、受信任 HTTPS 部署及 Windows/macOS 实际课堂使用仍需联调验收；安卓与 Windows、macOS 之间的蓝牙兼容方案尚未实现，须进行技术验证。

## 3. 核心概念

| 概念 | 定义 |
| --- | --- |
| 采样 | 从录音中切分、可独立试听和编辑的声音片段，最终长度不超过 1 秒 |
| 声音形象作品 | 一个动漫形象及其绑定的一个已编辑声音 |
| 本地作品库 | 学生设备上保存的作品集合，最多 200 份 |
| 等待区 | 显示未参与、等待加入及已退出角色的区域 |
| 演奏场景 | 显示正在参与演奏的角色及教师选定的背景 |
| Loop | 按节奏格子重复播放的一段音乐，长度为 1–16 小节 |
| 加入窗口 | 每播放完一遍完整 loop 后，下一遍开始的统一加入时刻 |

一遍 loop 是当前设置的完整小节数，并非固定 2 小节。例如设为 8 小节，一遍就是完整的 8 小节。新增角色的循环声部在当前这一遍结束、下一遍开始时加入，与已有声部一起播放。Loop 持续自动重复，不会在一遍或若干遍结束后主动停止，直到教师点击停止。

节奏修改、已接受作品替换及新增角色均在下一遍 loop 开始时生效。小节数修改仍保留此前确认的每两遍边界生效规则；用户此次修正的是加入窗口，不据此改变小节数修改时机。

## 4. 学生端

### 4.1 录音、自动切分与编辑

- 单次录音最长 15 秒，可以包含多次敲击或发声。
- 系统识别多次声音并自动切成独立采样，不只是去掉整段录音首尾静音。
- 学生逐个试听切片，自行选择需要的声音。
- 提供波形显示、试听和起止点裁切，允许手动修正自动切片。
- 每份作品最终绑定的采样最长 1 秒。
- 第一版不加入变调、混响等声音效果。

### 4.2 拍照与动漫形象生成

- 学生拍照，系统根据拍摄内容生成动漫虚拟形象。
- 全部角色采用统一动漫风格，使用透明背景以便放入课堂场景。
- 照片存在多个物体时，允许学生裁切或选择主体。
- 学生可以重新生成形象。
- 每个形象只绑定一个已经编辑好的声音。
- 生成服务已选用 OpenAI 图像 API，接口已接入；具体画风、耗时和透明背景效果仍需真实生成验收，尚未实现离线 AI 生成。

### 4.3 本地作品库与备份

- 最多保存 200 份声音形象作品。
- 支持命名、试听、删除，以及从作品库选择作品用于课堂。
- 本地保存不代表已经向教师提交。
- 支持本地备份、导出和导入，备份包含声音、形象及恢复作品所需的信息。
- 备份文件保存在本地，不要求云端作品备份。
- 当前实现采用 v2 JSON 整库备份，包含声音、裁切照片及动漫形象，并兼容 v1 声音备份。导入采用合并方式，相同 ID 跳过；新增作品导致总数超过 200 份或备份无效时整次拒绝，不部分写入。备份保存在学生设备本地。

### 4.4 课堂提交与更换

- 每位学生在一堂课中仅保留一份有效作品，即一个角色和一个声音。
- 允许学生提交更换申请，教师接受前原作品保持有效。
- 教师接受后，在下一遍 loop 开始时启用新声音和新形象，继承原节奏及参与状态。
- 替换作品不产生第二条独立的学生轨道，也不绕过原有参与规则。
- 同一学生存在多次待处理更换申请时，仅保留最新申请。
- 学生端显示发送中、已接收、更换待接受、已启用等状态。
- 传输失败可以重试，重连后恢复课堂状态。

## 5. 账号、入课与传输

### 5.1 账号与入课

- 需要账号登录。
- 教师创建课堂后生成二维码及课堂码，学生通过扫码或输入课堂码进入课堂。
- 首次注册或首次登录需要联网。
- 已登录过的设备可以离线进入课堂。
- 离线身份识别、登录凭据保存方式和有效期由后续技术方案明确。

### 5.2 网络与蓝牙

| 环境 | 产品要求 |
| --- | --- |
| 共同 Wi-Fi 且可访问互联网 | 支持常规课堂连接及作品传输 |
| 共同 Wi-Fi 但无互联网 | 已登录设备仍可进入课堂，支持局域网作品传输与课堂播放 |
| 无 Wi-Fi | 提供蓝牙备用通道，将声音和形象传给教师电脑 |

- 蓝牙需要覆盖安卓平板到 Windows、macOS 教师电脑的使用场景。
- 蓝牙传输成功后，作品仍遵守接收、替换审批和加入窗口规则。
- 断网不打断教师电脑已经接收的素材播放。
- 学生可以继续本地声音创作并在连接恢复后重试提交。
- 照片生成新角色是否能够离线完成尚未确定，不将“离线创作”视为离线 AI 生成已经实现。
- 蓝牙配对、课堂识别、多人传输队列和失败恢复的具体方案待技术验证。

## 6. 教师端课堂设置与布局

### 6.1 创建课堂

1. 输入本堂课人数，最大 50 人。
2. 选择采样环境背景，例如教室、厨房、操场。
3. 系统按人数准备场景位置。
4. 新收到的作品先进入等待区，同时自动预分配场景位置。
5. 教师选择参与角色并编排节奏。

允许教师增加课堂人数，但不得超过 50 人。课堂人数已满时，新学生等待教师扩容，不挤占已有学生位置。

### 6.2 位置与展示

- 教师可以手动拖动角色位置，覆盖自动预分配位置。
- 场景仅显示正在参与的角色；退出角色回到等待区。
- 手动拖动只调整空间位置，不改变其节奏编排或加入时间。
- 全部角色使用统一的声音触发动作，具体动作在原型阶段确定。
- 每次声音触发时，对应角色同步执行动作；多人同时发声时，各自角色同时响应。

### 6.3 页面区域

- 演奏场景为主要展示区域。
- 等待区放在场景旁，显示未参与、等待加入及退出角色。
- 下方放置可收起的节奏格子及播放控制。
- 提供全屏场景展示模式。
- 显示循环进度和等待加入提示。

## 7. 节奏与播放控制

### 7.1 节奏格子

- 固定 4/4 拍，每小节 16 格，每格对应一个十六分音符。
- 第一版不提供三拍子或其他节奏细分。
- 每位学生的声音可以独立编排，使用格子控制触发时刻。
- 播放中修改节奏，当前这一遍保持原编排，下一遍 loop 开始时生效。
- 教师端需要区分当前播放内容与待生效修改。

### 7.2 参数

| 控制项 | 范围或定义 | 生效时机 |
| --- | --- | --- |
| 速度 | 40–240 BPM，默认 100；滑块、加减按钮和数值输入 | 实时 |
| Swing | 作用于成对的十六分音符，默认无 Swing，通过滑块调整 | 实时 |
| 总音量 | 控制整体输出响度 | 实时 |
| 单角色音量 | 独立调整每位学生声音大小 | 实时 |
| 小节数 | 1–16 的任意整数 | 播放中在下一个两遍循环边界生效 |

Swing 最大程度和音量数值范围在原型阶段确定。速度变化不重置音乐进度；所有角色继续共享同一循环位置。

### 7.3 小节数变化

- 一遍 loop 可选择 1 至 16 小节，不局限于 1、2、4、8。
- 播放中调整时，先完成当前长度的两遍循环组，再应用新长度。
- 缩短从末尾向前删除超出范围的小节及其节奏，不保留隐藏小节。
- 后续增加长度不能自动恢复已经删除的节奏。
- 例如从 8 小节缩短为 5 小节，删除第 6–8 小节。
- 一遍为 2 小节时，每播放完 2 小节到达一次加入窗口；一遍为 8 小节时，每播放完 8 小节到达一次加入窗口。长度变化后按新长度计算后续加入窗口。

### 7.4 播放与停止

- 只提供播放和停止，不提供暂停。
- 首次播放前，教师可以预先选定参与角色；这些角色直接参与第一遍。
- 停止立即结束播放，进度回到起点，保留节奏编排和已选角色。
- 再次播放从起点开始。
- 每遍 loop 结束后自动从头继续播放，循环边界不是停止点；只有教师点击停止才主动结束播放。
- 停止时尚未生效的更换、节奏、小节数及等待加入操作如何处理，见待定边界事项。

## 8. 角色状态与完整 Loop 加入规则

### 8.1 状态

| 状态 | 所在区域 | 声音行为 |
| --- | --- | --- |
| 未参与 | 等待区 | 不播放 |
| 等待加入 | 等待区，显示等待标记 | 暂不播放 |
| 正在参与 | 演奏场景 | 按个人节奏格子循环播放 |
| 已退出 | 回到等待区，场景中不再显示 | 立即停止 |

### 8.2 加入

1. 全体角色使用统一循环进度，每遍覆盖当前设置的全部小节。
2. 播放中教师选择角色加入后，该角色等待当前这一遍 loop 结束。
3. 下一遍开始时，新角色进入场景，与持续循环的已有角色一起演奏，无需等待额外一遍。
4. 教师可以在角色正式加入前取消等待。
5. 以教师端实际收到操作的时间判定；错过边界则等待下一个窗口。
6. 退出后再次加入也遵守相同规则。
7. 已经参与的角色跨窗口持续演奏，不需要重新选择。

示例：当前设置为 8 小节，第 3 小节选择 A、第 6 小节选择 B；播放完第 8 小节后，下一遍第 1 小节开始时 A、B 同时加入。已有角色继续循环，整体播放不会停止。

### 8.3 退出

- 教师可以随时令角色退出，无需等待 loop 结束。
- 立即停止该角色发声，形象从场景移回等待区。
- 其他角色继续播放，循环进度不重启。

### 8.4 进度条

必须提供进度条提示当前一遍完整 loop 的进展及下一次加入时机，且随实时速度变化推进。进度条覆盖当前设置的全部小节，每遍结束后归零并立即继续推进，不表示停止播放。

展示细节建议：标明当前小节／总小节数及拍点，并显示等待加入状态；如显示剩余秒数，需要随速度变化更新。具体视觉样式在原型阶段确定。

## 9. 操作生效时机总表

| 操作 | 时机 |
| --- | --- |
| 开始播放前预选角色 | 第一遍起点参与 |
| 播放中新增或重新加入角色 | 当前完整 loop 结束、下一遍开始时 |
| 取消等待加入 | 正式加入前可以取消 |
| 退出角色 | 立即停止并回到等待区 |
| 修改节奏格子 | 下一遍 loop 开始 |
| 教师接受作品替换 | 下一遍 loop 开始，继承原节奏和参与状态 |
| 修改速度、Swing、总音量或单角色音量 | 实时 |
| 修改小节数 | 下一个两遍循环边界 |
| 停止 | 立即结束播放并归零，保留编排和已选角色 |

本表中的循环边界规则针对播放状态；停止状态下的编辑及待生效操作处理需补充统一细则。

## 10. 典型课堂流程

1. 教师登录，设置人数和背景，生成课堂码及二维码。
2. 学生登录后进入课堂；已登录过的设备在无互联网时也能进入。
3. 学生录制不超过 15 秒的声音，自动切分后试听并编辑一个不超过 1 秒的采样。
4. 拍照选择主体，生成统一风格的动漫形象，与采样绑定并保存本地。
5. 每位学生选择一份作品提交，通过 Wi-Fi 或蓝牙传输。
6. 教师端接收后放入等待区、预分配位置，教师可以手动拖动布置。
7. 教师编排节奏，选择 1–16 小节长度和首批角色，开始播放。
8. 教师实时调整速度、Swing 和音量；格子修改下一遍生效。
9. 新角色在当前完整 loop 结束、下一遍开始时加入，退出角色立即回等待区。
10. 学生申请更换作品，教师接受后于下一遍切换。
11. 教师停止时播放归零，编排和已选角色保留。
12. 学生可将作品库备份导出到本地，并通过导入恢复。

## 11. 验收要点

| 编号 | 验收场景 | 预期结果 |
| --- | --- | --- |
| A01 | 录制多次清晰敲击 | 自动切成多个可试听、选择和手动修正的采样 |
| A02 | 音频时长 | 单次录音不超过 15 秒，最终单个采样不超过 1 秒 |
| A03 | 本地作品管理 | 支持命名、试听、删除，最多 200 份 |
| A04 | 本地备份恢复 | 导出及导入能恢复声音、形象及绑定关系，不依赖云端备份 |
| A05 | 形象生成 | 统一风格，可选择主体并重新生成 |
| A06 | 课堂提交 | 每位学生仅有一份有效作品和一个声音 |
| A07 | 作品更换 | 教师接受前保持原作品，接受后下一遍替换并继承节奏和参与状态 |
| A08 | 重复更换 | 仅保留最新待处理申请，显示明确状态 |
| A09 | 首次接收和位置 | 进入等待区并预分配位置，教师可拖动调整 |
| A10 | 最大规模 | 50 位学生声音可同时按各自节奏循环播放 |
| A11 | 首次播放 | 预选角色从第一遍起点参与 |
| A12 | 播放中加入 | 任意一遍中提出的加入均在当前完整小节数播放结束后，于下一遍起点生效 |
| A13 | 取消及重新加入 | 等待可取消；退出后再次加入仍等窗口 |
| A14 | 立即退出 | 停止该角色发声并移回等待区，不打断其他角色 |
| A15 | 节奏修改 | 当前遍不变，下一遍应用 |
| A16 | 实时控制 | 速度、Swing、总音量及单角色音量实时变化 |
| A17 | 小节数 | 支持 1–16 所有整数，播放中于两遍边界应用 |
| A18 | 缩短循环 | 从末尾删除超出小节，扩大后不恢复已删节奏 |
| A19 | 停止 | 无暂停功能；停止归零并保留编排及已选角色 |
| A20 | 进度与动画 | 进度跟随实际音乐速度；角色按声音触发统一动作 |
| A21 | 账号与离线 | 首次注册或登录需联网，已登录设备可离线入课 |
| A22 | 无互联网的共同 Wi-Fi | 可传输作品并完成课堂播放 |
| A23 | 蓝牙备用传输 | 安卓分别向 Windows、macOS 传输声音和形象，保留课堂审批规则 |
| A24 | 断网恢复 | 教师已接收声音继续播放；学生可本地编辑、重试提交并恢复状态 |
| A25 | 人数扩容 | 满员学生等待教师扩容，人数不超过 50 |
| A26 | 完整 Loop 与连续播放 | 分别设置 1、2、8、16 小节，均在完整播放所设小节数后开放加入窗口；无人操作时连续重复播放，不主动停止 |

技术验收需覆盖常规 15 人及上限 50 人。自动切分准确率、音画同步误差、播放稳定性、传输耗时和操作延迟的量化目标尚需制定。

## 12. 剩余待定事项与技术验证

已经确认的要求不在本节重新列为待确认。本节仅保留具体实现、视觉参数及未讨论的边界行为。

| 类别 | 剩余事项 |
| --- | --- |
| 视觉与试听 | 统一动漫画风的具体样式、统一动作、Swing 最大程度、音量范围和默认值 |
| 声音处理 | 已使用浏览器录音、单声道 PCM WAV 采样及能量起点切分；仍需在真实课堂验证误切率、噪声场景和设备兼容性 |
| 形象生成 | OpenAI 图像 API 已接入，尚未配置真实密钥验收；需确认实际画风、透明背景、生成耗时及失败体验，离线生成未实现 |
| 蓝牙 | 安卓到 Windows 和 macOS 的协议、配对、设备发现、权限与兼容性验证 |
| 多人传输 | 50 人环境下的传输队列、失败重试、重复数据识别及完整性校验 |
| 离线课堂 | 无 Wi-Fi 时课堂码与蓝牙设备的对应方式、缓存身份验证、离线凭据有效期 |
| 停止状态 | 停止时待生效节奏、替换、小节数和等待加入的处理；停止期间编辑的生效方式 |
| 事件边界 | 同一窗口同时发生退出、替换、加入及长度变化时的确定性处理顺序 |
| 本地备份 | 已实现 v2 JSON 整库备份、合并导入、相同 ID 跳过和超额整次拒绝；带声音及图片的备份恢复仍需安卓实际文件操作验收 |
| 布局 | 编号位置保存与刷新恢复已实现；15 人与 50 人的实际角色尺寸、轨道滚动和全屏演奏展示仍待后续模块实现与验证 |
| 性能指标 | 目标安卓设备及电脑配置、最低系统版本、延迟与音画同步目标 |
| 账号细节 | 注册方式、身份展示、教师与学生权限及账号恢复流程 |

## 13. 本轮范围与版本变更

### 13.1 当前开发进度

当前使用浏览器前端、本地 Node.js 课堂服务及 SQLite 数据库。学生作品在浏览器 IndexedDB 中保存，明确提交后才传给课堂服务；账号、课堂及已提交素材由运行服务的电脑保存。

| 模块 | 当前进度与边界 |
| --- | --- |
| 声音采集与编辑 | 已实现最长 15 秒录音、自动切片、试听、波形拖动裁切及最长 1 秒采样；自动切分效果与安卓麦克风仍需真机验收 |
| 本地作品与备份 | 已实现最多 200 份作品、命名、试听、删除、声音与图片绑定，以及 v2 JSON 整库备份和合并恢复 |
| 照片与动漫形象 | 已实现拍照入口、图片导入、主体裁切及 OpenAI 接口；测试使用模拟响应，尚未配置真实 API 密钥验证生成效果 |
| 账号与课堂提交 | 已实现学生/教师注册登录、课堂码与二维码链接、提交、更换审批、最新申请保留及本地待发送队列；跨设备扫码仍需验证 |
| 教师课堂准备 | 本轮实现课堂统计、背景预览、编号位置、作品等待区、位置调整与持久化，以及稳定编号和扩容布局 |
| 教师节奏与演奏 | 尚未接入应用；节奏格子、播放/停止、速度/Swing/音量、完整 loop 加入与替换、统一触发动画均不作为当前已交付功能 |
| 离线与蓝牙 | 页面已加载后可本地采集、编辑和保存；课堂服务不可达时可准备待发送作品。离线重新打开应用、完整离线入课及蓝牙传输未完成 |

当前教师接受更换仅更新课堂接收素材，尚未执行“下一遍 loop 启用”或向学生回报真实演奏中的“已启用”状态。后续播放模块必须实现第 7–9 节已确认规则，包括完整 loop 边界加入、持续循环及两遍边界修改小节数。

### 13.2 本轮教师小模块：课堂设置与作品等待区

本轮只交付课堂准备与作品接收，按以下流程使用：

1. 教师创建课堂时设置 1–50 人及教室、厨房或操场环境，获得课堂码和二维码。
2. 页面显示已入课、已收到作品、尚未提交、待处理更换和等待扩容统计，并以背景预览展示对应数量的编号预留点。
3. 已入课学生获得稳定编号；提交作品后在等待区展示声音与形象，可试听和处理更换申请。场景仅显示编号，未参与演奏的角色仍留在等待区。
4. 教师可拖动编号或使用方向键调整预留位置；服务器保存坐标，刷新后保持。保存失败时显示提示并恢复原位置。
5. 扩容只新增预留点，不移动旧位置；等待学生按稳定入课顺序获得新增名额。重复入课、刷新和更换作品不改变已分配编号。

编号预留点是当前课堂准备模块的实现方式，不替代第 6.2 节关于参与角色、退出角色及声音触发动作的正式需求。节奏与播放的开发草稿未纳入本轮发布，后续分模块接入。

已有自动化验证覆盖现有基础流程、位置权限、稳定编号、扩容、历史数据迁移与持久化；这不等同于学生端、教师端或全部验收条目已完成。真实 OpenAI 生成、安卓相机与麦克风、HTTPS 和跨设备扫码、15/50 人课堂负载及 Windows/macOS 蓝牙仍需后续实施或验收。

### 13.3 版本记录

v0.4 更新实际开发进度，记录已选用 OpenAI 图像 API、网页实现、本地备份规则及教师“课堂设置与作品等待区”模块；保留已确认的完整 loop、两遍边界及其他正式需求。

v0.3 修正 loop 定义及加入窗口：一遍覆盖当前设置的完整小节数，每遍结束即可加入新角色，并明确持续循环至教师停止。小节数修改的生效时机保留此前确认规则。

v0.2 将已确认建议转为正式需求，补充 Windows/macOS、安卓、账号与离线入课、Wi-Fi/蓝牙传输、本地备份、50 人上限及素材限制。

明确替换两项旧方案：

- 删除暂停功能，只保留播放与停止。
- 小节数改为 1–16 的任意整数；缩短时删除末尾小节，不再保留隐藏编排。

云端作品备份不是当前要求；课堂存档、成品音频导出、乐谱导出和云端跨设备同步尚未纳入已确认范围。课堂位置的自动保存仅用于恢复本模块布局，不表示完整课堂演奏存档已经实现。
